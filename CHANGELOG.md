# 更新日志

本项目遵循[语义化版本](https://semver.org/lang/zh-CN/)。版本节奏与 `docs/02-产品需求文档.md` 对齐。

## [未发布]

## [2.2.0] — 2026-08-14 · 界面与阅读体验

这一版没有新的 Git 能力，全部投在「每天要盯着的那几屏」上：提交历史、
diff、变更列表、侧栏。另外补上了云服务的服务端。

### 新增

- **diff 语法高亮**，覆盖 Swift / JS·TS / Python / Go / Rust / Java·Kotlin /
  C·C++·ObjC / Ruby / Shell / JSON / YAML / SQL。只做词法不做语法，
  只处理单行不跨行——hunk 给出的就是文件中间几行，跨行状态本来就无从维护
- **行内词级差异**：同一行只改了一个词时，不再整行标红标绿，
  而是标出真正变化的那几段。中文按单字比较
- **提交详情列出改动的文件**，点一行看它在这次提交里的 diff，
  上下用 VSplitView 分隔可拖动
- **提交历史按信息、作者、时间筛选**，交给 git 去筛而不是过滤已加载的那几百条
- **变更列表按目录分组**，单子目录链自动合并；目录行可一键暂存整个目录
- **侧栏过滤框**，分支改按最近提交时间排序，当前分支置顶
- **diff 折行开关**，长行不必再横向拖
- 提交列表支持 j/k 移动与 ⌘C 复制 hash
- **云服务网关**（`server/`）：订阅制 AI 服务的服务端，Go + PostgreSQL，
  实测常驻 11MB。只存 token 用量，不存任何用户代码与请求内容

### 修复

- 切换提交时弹「操作失败 CancellationError()」。`.task(id:)` 在 id 变化时
  取消上一个任务，而取消被当成了失败
- 选中行的分支图消失：轨道配色第一个是 systemBlue，而选中背景也是强调色蓝，
  蓝线画在蓝底上等于没画
- 提交列表左边界一行一个样、滚动时像在左右抖：图形区宽度约束与标题的
  压缩阻力优先级打平，取舍取决于标题长短
- 提交标题被挤到几乎看不见：图形区按**整个已加载历史**的最大并行轨道数
  留宽，哪怕眼前这一屏全是单线提交
- 工具栏在三栏都展开时塌进 ›› 溢出菜单

### 变更

- 建立 `Theme.swift` 统一视觉规范，颜色一律用系统语义色
- 提交详情不再强行显示 40 位完整 hash，改为短 hash + 复制按钮


## [2.1.0] — 2026-08-13 · 远期方向落地

规划文档里列在「远期」的两项。至此 `docs/03-实现计划.md` 中的全部内容
（v0.1 → v2.0 里程碑 + 远期方向）实现完毕。

### 新增

**对话式 Git 操作**

- 说一句话 → AI 给出操作计划 → 预览确认 → 执行，每一步都能撤销
- 模型**只能从白名单动作里选**，永远拿不到「生成 git 命令字符串然后执行」
  的能力——那条路等于把命令注入的入口开给一个会被提示词左右的东西
- 参数过两道校验：AIKit 校验「说的对不对得上仓库状态」，App 侧校验
  「翻译出来的是不是一条合法的 git 操作」
- 预览不可跳过，每步显示等价 git 命令；执行走 `RepoActor.perform`，
  所以进时间线、可撤销
- 模型听不懂时可以反问——让它硬凑一个计划出来，比直说「我不确定」危险得多

**AI 云服务（客户端侧，订阅制）**

- 订阅状态与额度模型、设置页集成
- 协议上复用 OpenAI 兼容层：云服务与自带 Key 走同一条经过测试的代码路径
- **服务端尚未上线，界面上如实标注**，不假装能用

### 说明

商业模式边界不变（PRD §6）：本地 Git 全功能永久免费，云服务只对
有边际成本的 AI 推理收费。不订阅可用全部本地功能，也可自带 Key
使用全部 AI 功能。

## [2.0.0] — 2026-08-13 · AI 指挥中心

驭Git 从「一个好用的 Git 客户端」变成「多 agent 时代的 Git 指挥中心」。

### 新增

**并行工作区（支柱 3）**

- 同一仓库同时签出多个分支到不同目录，给每个 agent 分一个，互不踩踏
- 状态卡片一眼看出领先/落后/未提交数，「可以合了」直接标出来
- 建 → 看状态 → 合回来 → 清理，一条流水线走完
- 目录被删掉的记录仍然列出，否则用户不知道该清理它

**AI 归因 blame**

- 逐行区分人写与 AI 参与，按工具着色并给出占比
- 只看署名类的行判定，正文里提到 AI 不算——把人写的代码标成 AI，
  比漏标更让人恼火
- 认不出的工具一律算人工，宁可少报不错报

**平台集成**

- GitHub / GitLab（含自建）/ Gitee 的 PR / MR 列表与创建
- 只做列表和创建：评审讨论在网页上做得更好，硬搬进来只是个更差的浏览器
- 令牌按主机名存 Keychain，同时管公司自建实例和公有云互不干扰

**教学模式**

- 危险操作预警固定回答三个问题：会发生什么、能不能撤销、怎么撤销
- 改写历史与丢弃改动分成两类文案——后者 reflog 也救不回来，必须点明
- 确认按钮上写动作而非「确定」，让人再读一遍自己要做什么
- 新手引导按真实工作流排序，概念解释挂在用到它的那一步旁边

### 修复

- `URL.path()` 默认返回 percent-encoded 形式，GitLab 的中文 group 名
  会变成一串 `%E7%BB%84`
- worktree 列表不加 `-z` 时，中文路径 / 分支名 / 锁定原因都会被
  C 风格八进制转义

## [1.0.0] — 2026-08-13 · 正式发布：主打故事完整

四根支柱都立起来了：无畏 Undo、按意图拆分提交、AI 解冲突、本地 AI 评审。

### 新增

**AI Commit Composer（支柱 2）**

- 按「做的是哪件事」而不是按文件把改动分组，每组提交一次
- AI 给的是提议：标题能改、块能挪到别的组、整组能删；删组时里面的
  改动退回待分配，不会跟着消失
- 未分配的改动单独列出并明确提示「会留在工作区」，绝不悄悄丢
- 逐批提交前清空暂存区，各批互不串味

**内置三方合并编辑器 + AI 逐处解冲突（支柱 4）**

- 我方 / 共同祖先 / 对方三栏并排；读冲突前自动用 `--conflict=diff3`
  补出共同祖先——没有它就没法判断双方各改了什么
- AI 逐处给建议：中文理由 + 置信度色标。高置信度同样不自动应用，
  色标只帮用户决定哪几处可以快速扫过
- 支持要我方 / 要对方 / 两个都要 / 都不要 / 手动编辑
- 写回时内容里还有冲突标记则拒绝——git add 不检查这个

**提交前本地 diff 评审（支柱 4）**

- 中文摘要 + 逐条意见，按风险分级：鉴权、密钥、数据删改、并发置顶，
  纯格式化垫底且默认折叠
- 评审看暂存区而非工作区，只评将要提交的内容
- 永远不阻断提交——它是自查工具，不是门禁

### 修复

- 分批提交时 hunk 下标会随每次提交失效（HEAD 前移后 diff 重新计算），
  改为所有 patch 在动手前一次性生成
- 已经 `git add` 过的改动在 `git diff` 里看不见，会被整批漏掉；
  新增 `diffAgainstHead` 取相对 HEAD 的全部改动
- CRLF 文件里 `=======\r` 不被识别为冲突标记——CR 属于行结束符，
  不该参与标记匹配

## [0.5.0] — 2026-08-13 · 公开 Beta：第一个传播点

时间线、Command Palette、AI 基础能力、可视化 rebase 四块齐了。
这一版的主张是：**Git 里最让人怕的几件事，现在都有退路**。

### 新增

**仓库时间线与无畏 Undo（支柱 1）**

- 危险操作前自动拍工作区快照，存进 `refs/yugit/timeline/` 私有命名空间——
  不污染 stash 栈、不进 `git log`、不会被 push，但 ref 存在即免于 gc
- 全局 ⌘Z 退回上一步之前的工作区状态；撤销本身也留快照，可以再撤销
- 终端里 `git reset --hard` 造成的破坏同样能从时间线恢复：
  外部改动经 FSEvents 检测后静默期打点

**Command Palette 与透明命令层**

- ⌘K 唤起，子序列匹配（输入 tj 也能命中「提交」）
- 每条命令旁显示等价的 git 命令，取自 `GitOperation` 元数据——
  面板、时间线、教学提示三处的说明天然一致

**AI 基础能力（BYOK）**

- 双协议：Anthropic 原生 + OpenAI 兼容（一份实现覆盖 OpenAI、DeepSeek、
  Kimi、通义、智谱、本地 Ollama）
- API Key 只进 Keychain，不参与 iCloud 同步；请求直连用户填的服务商，
  不经过任何中转服务器
- AI 起草提交信息，流式进提交框且可编辑；中文解释 commit 与 diff
- 发出去的内容先过三关：敏感文件整份排除、疑似密钥打码、超预算按文件截断。
  每一关做了什么都如实显示给用户，并一并写进提示词
- 没配置 AI 时所有 AI 入口都不出现，不是灰掉是不存在

**可视化 interactive rebase**

- 拖动排序 + 下拉选动作，每个动作旁写清楚会发生什么
- 全程不弹编辑器：todo 事先生成，经 `GIT_SEQUENCE_EDITOR` 拷入
- 改写历史前自动打 `refs/tags/yugit-backup/` 下的备份 tag
- 卡在冲突时顶部挂常驻横幅，把出路直接做成按钮
- Quick Actions：右键提交一步完成合并进父提交 / 改写信息 / 丢弃

### 修复

- SSE 解析改为字节层面切行：规范允许 `\n`、`\r\n`、`\r` 三种行结束符，
  而 Swift 把 `"\r\n"` 当作一个 Character，`String.split` 切不开
- diff 路径解析改用 `+++ b/` 行：`diff --git` 行在路径含空格时真有歧义
- `git log --grep` 与 `--author` 是 AND 不是 OR，改为分别查询后合并

## [0.3.0] — 2026-08-13 · 内部 Alpha：daily driver

分支、远程、历史三块补齐，日常工作可以不再回终端。

### 新增

**分支**

- 新建、切换、重命名、删除、合并、设置 upstream
- 用 `git switch` 而非 `checkout`，语义收敛不会意外 detach
- 强制删除标记为改写历史，并说明删后只能靠 reflog 找回

**远程**

- fetch / pull / push / clone，传输过程实时显示中文进度
- 失败诊断把 git 的英文报错识别成六类，每类给出下一步该做什么：
  认证失败提示「平台早已不收账号密码，要用 token」，
  推送被拒警告「不要直接用 force，那会覆盖别人的提交」
- 只提供 `--force-with-lease`，不提供无租约的 `--force`

**历史**

- 提交历史换成 AppKit `NSTableView`，行复用让 5 万 commit 仍能流畅滚动
- 分支图用 CoreGraphics 自绘，分叉处走曲线
- 滚动到底部自动增量加载
- 打开仓库时后台预热 commit-graph 缓存

**搜索**

- ⇧⌘F 全仓库即时搜索，可按内容、文件名、提交三种范围
- 内容搜索用 `git grep`，天然跳过 .gitignore 里的构建产物
- 输入防抖并取消过期查询

### 修复

- 缺少 `CFBundleDocumentTypes` 声明导致无法从 Finder 拖拽仓库打开
- `git log` 的 `--grep` 与 `--author` 之间是「与」而非「或」，
  同时给两个条件会一条都搜不到

### 性能

5 万 commit 基准仓库实测：

| 操作 | 耗时 | 门槛 |
|---|---|---|
| 界面完整刷新 | 63ms | < 1s |
| 历史首屏 200 条 | 48ms | < 500ms |
| 拓扑序首屏（画分支图用） | 67ms | < 500ms |
| App 内存占用 | 143MB | < 500MB |

拓扑序原本要 370ms，写入 commit-graph 缓存后降到 67ms。

### 已知限制

- 冲突只能标记，逐 hunk 解决冲突的界面在 v1.0
- 交互式 rebase 在 v0.5
- 时间线 Undo 在 v0.5

## [0.2.0] — 2026-08-13 · 内部 Alpha：提交工作流

从只读浏览进到能干活。可以看 diff、逐 hunk 或逐行暂存、提交与 amend，
外部改动实时反映到界面。

### 新增

**diff 与暂存**

- **DiffParser / PatchBuilder**：解析与生成构成同构关系，用往返测试锁死。
  这是 hunk / 行级暂存不损坏用户文件的根本保证
- 整文件、hunk、行三个粒度的暂存与取消暂存
- 丢弃改动、stash push / pop
- diff 查看器：行号、增删标记、逐 hunk 暂存按钮，超 2000 行先折叠

**实时性**

- **RepositoryWatcher**：基于 FSEvents，300ms 防抖，写操作期间可挂起。
  终端里的 git、编辑器保存、agent 写的代码都能立刻反映到界面

**提交**

- 提交面板，amend 时自动带出上一条说明并提示 hash 会变

### 修复的隐患

- **CRLF 文件的 diff 被整块吞掉**：Swift 把 `\r\n` 当作单个 Character，
  `String.split(separator: "\n")` 完全不分割。改为按字节切分，
  行尾的 `\r` 也因此原样保留，暂存不会把换行符从 CRLF 改成 LF
- **无尾换行的文件被补上换行**：`\ No newline at end of file` 标记
  必须随 patch 原样带上
- **历史首屏慢 7 倍**：`--date-order` 带拓扑约束，要遍历完整提交图。
  改用 git 原生排序，界面刷新 396ms → 63ms

### 性能

5 万 commit、400 个工作区文件的基准仓库实测：

| 操作 | 耗时 | 门槛 |
|---|---|---|
| 界面完整刷新 | 62ms | < 1s |
| 历史首屏 200 条 | 48ms | < 500ms |
| 单文件 diff | 44ms | < 200ms |
| 增删行数统计 | 49ms | < 500ms |

### 已知限制

- 分支切换、fetch/pull/push 在 v0.3
- 历史列表用 SwiftUI 实现，大仓库滚动性能待 v0.3 换 AppKit 虚拟化列表
- 冲突只能标记，解决冲突的界面在 v1.0

## [0.1.0] — 2026-08-13 · 内部 Alpha：只读浏览

第一个能跑起来的版本。可以打开仓库，看到分支、标签、工作区变更与提交历史。

### 新增

**Git 引擎（GitKit）**

- **ProcessRunner**：子进程执行器，并发排空 stdout/stderr 防管道死锁，支持超时与取消
- **GitClient**：git CLI 调用层，隔离继承环境、禁止交互挂起、避免 index.lock 竞争
- **StatusParser**：porcelain v2 解析器，覆盖重命名跨段、中文与含空格路径、冲突、submodule
- **LogParser**：提交历史解析，自行解析 ISO 8601 时间戳以满足首屏性能预算
- **RefParser**：分支与 tag 解析，区分附注/轻量 tag，识别 upstream 的 gone 与分叉状态
- **RepoActor**：仓库写操作的单一入口，显式串行化并记录操作日志
- **GitOperation**：操作描述类型，自带中文摘要、注解与等价 git 命令
- **FileOperationLog**：JSONL 操作日志，为 v0.5 的时间线 Undo 打底

**界面**

- 三栏工作区：分支/标签侧栏 ｜ 变更与历史 ｜ 详情
- 欢迎页与最近打开列表
- 支持从 Finder 拖拽或 `open` 命令打开仓库

**工程**

- 工程规范（`docs/04-工程规范.md`）与本地质量门禁（swift-format 配置、git hooks）

### 性能

5 万 commit、400 个工作区文件的基准仓库实测（门槛见 `docs/04-工程规范.md` §8）：

| 操作 | 耗时 | 门槛 |
|---|---|---|
| status | 56ms | < 1s |
| 历史首屏 200 条 | 51ms | < 500ms |
| 分支列表 | 47ms | < 200ms |
| 界面完整刷新 | 63ms | < 1s |

### 已知限制

- 只读。暂存、提交、分支操作在 v0.2
- diff 查看器在 v0.2
- 历史列表用 SwiftUI 实现，大仓库滚动性能待 v0.3 换 AppKit 虚拟化列表
