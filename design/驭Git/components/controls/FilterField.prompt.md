过滤框，放在它作用的那一列顶部，不放工具栏。

```jsx
<FilterField value={filter} onChange={setFilter} placeholder="过滤分支与标签" />
```

- 子串匹配，忽略大小写与变音符号；**不要模糊匹配** —— 分支名里的 `-` `/` 有结构含义，fuzzy 会让 `dev` 命中 `d-e-v`。
- 文件不多（≤8）时不显示它，省一行高度。
- 有内容时右侧出现清空按钮，清空后焦点留在输入框里。
