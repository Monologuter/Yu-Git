工具栏上的图标按钮，28×24，hover 才出底色。

```jsx
<ToolbarButton icon="arrow.down.circle" title="从远程拉取引用与对象，不改动工作区" />
<ToolbarButton icon="clock.arrow.circlepath" title="时间线" active />
```

工具栏一共只留四项：获取、拉取、推送、更多。其余动作进「更多」菜单或命令面板 —— 空间不足时让系统替你取舍，结果会很难看。
