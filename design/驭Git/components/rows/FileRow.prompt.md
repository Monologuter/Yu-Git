变更列表里的一行文件：状态字母 + 文件名（+ 目录）。

```jsx
<FileRow status="modified" name="StoryboardVideoController.java" directory="ai-apis/ai-web-api/src/main/java/com/tvjoy" />
<FileRow status="unmerged" name="routes.ts" selected />
```

整行可点，不只是文字 —— 目标小的点击区最招人烦。冲突文件（`unmerged`）要能在分组里被单独拎出来，并在分组头给一个「解决…」直达入口。
